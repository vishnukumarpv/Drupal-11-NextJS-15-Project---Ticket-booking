services:
  ticket_booking_app.booking_normalizer:
    class: Drupal\ticket_booking_app\Normalizer\BookingNormalizer
    arguments: ['@entity_type.manager', '@entity_type.repository', '@entity_field.manager']
    tags:
      - { name: normalizer, priority: 10 }
